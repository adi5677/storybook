<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.12.0-2/css/all.min.css"
        integrity="sha256-46r060N2LrChLLb5zowXQ72/iKKNiw/lAmygmHExk/o=" crossorigin="anonymous" />
    <link rel="stylesheet" href="/css/style.css">
    <title>StoryBooks</title>
</head>

<body>
    <%- include('../partials/_header') %>
    <%- include('../partials/_add_btn') %>
    <div class="container">

        <div class="row">
            <div class="col s12"></div>
            <div class="col s12 m4 l2"></div>
            <div class="col s12 m4 l8 center-align">
                <h3><%= story.title %></h3>
                <div class="card story">
                    <div class="card-content">
                        <span class="card-title"> <%= new Date(story.createdAt).toLocaleDateString() %></span>
                        <%= story.body %>
                        <p><b>Tags: <%= story.tags %></b></p>
                        <a href="/api/stories/<%= story._id %>/edit" class="btn green">Edit</a>
                        <a href="/api/stories/<%= story._id %>/delete" class="btn red">Delete</a>
                    </div>
                </div>
            </div>

            <div class="col s12 m4 l2"></div>
          </div>
    </div>
</body>

</html>

